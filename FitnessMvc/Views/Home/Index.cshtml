@model IEnumerable<FitnessMvc.Models.Todo>

@{
    ViewBag.Title = "Home Page";
}

<ul>
    @foreach (var todo in Model)
    {
        <li>@todo.Title - @todo.CreateDate - @todo.User.Name</li>
    }
</ul>